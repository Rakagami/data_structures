#include "list.h"

#include <stdio.h>
#include <stdlib.h>

struct LinkedListNode* LLaddValue(int value) {
    return NULL;
}

struct LinkedListNode* LLdeleteValue(int value) {
    return NULL;
}

int main() {
    printf("This is the list.c as an executable\n");
}
